# ISSUES
- Padlock is a fixed color `replace image depending on theme`
- Some icons have white internal coloring `needs to be empty`
- Toggling theme removes pseudo-class styling `create better implementation`
- Prematurely dropping lock results in breaking `debug code`
- Short verticality results in white space at bottom for larger screens `fix footer at bottom`